let k = [2, 4];

console.log('k: ', k);
let eachElement = [];
let zahl = 0;
for (let i = 0; i < k.lenght; i++) {
  let newArray = new newArray(k[j]);
  for (let j = 0; j < k[i]; i++) {
    newArray[j] = j + 1;
  }
  console.log('newArray: ', newArray);
}

for (let i = 0; i < k.lenght; i++) {
  let number = k[i];
  let array = new Array(number);

  for (let j = 0; j < number; j++) {
    array[j] = j + 1;
  }
  console.log('Array: ', array);
}
